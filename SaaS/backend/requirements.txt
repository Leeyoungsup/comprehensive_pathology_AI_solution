# ── 웹 서버 ──
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
aiofiles==24.1.0

# ── 이미지/WSI ──
openslide-python==1.3.1
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# ── 과학 계산 (VS-IHC tissue mask: skimage morphology + scipy fill_holes) ──
numpy>=1.26.0
scipy>=1.11.0
scikit-image>=0.22.0

# ── 딥러닝 (YOLOv11 detection + VS GAN + tissue segmentation) ──
# CUDA 빌드는 install.bat 에서 pytorch 공식 index 로 별도 설치
torch>=2.0.0
torchvision>=0.15.0
segmentation-models-pytorch>=0.3.3

# ── 레거시 호환 (ai/virtual_stain.py, ai/segmentation.py 가 QThread/pyqtSignal 을
#    모듈 레벨에서 import 하므로 SaaS 환경에서도 필요) ──
PyQt5>=5.15.0
